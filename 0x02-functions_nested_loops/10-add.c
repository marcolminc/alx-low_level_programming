#include "main.h"

/**
 * add - adds two integers and return the result
 * @a: the first operand
 * @b: the second operand
 *
 * Return: the sum of a and b
 */
int add(int a, int b)
{
	return (a + b);
}
