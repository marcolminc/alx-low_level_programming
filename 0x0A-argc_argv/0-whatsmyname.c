#include "main.h" 


/**
 * main - prints the name of the executable for this program
 * @argc: number of CLI args
 * @argv: vector of CLI args
 *
 * Return: nothing
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
