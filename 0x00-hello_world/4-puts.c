#include<stdio.h>

/**
 * main - prints a sentence to console
 * Return: always 0
 */

int main(void)
{
	/* the sentence to be printed exactly as it is to the console */
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
