#include "main.h"

/**
 * _abs - returns absolute value of a number.
 * @n: the number whose absolute value should be returned.
 *
 * Return: absolute value of n.
 */
int _abs(int n);
{
	if (n < 0)
		return (n * -1);
	return (n);
}
